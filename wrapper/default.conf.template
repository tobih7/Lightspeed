server {
    listen       80;
    listen  [::]:80;
    server_name  Lightspeed;

    location / {
        root /usr/share/nginx/html;
        index  index.html index.htm;
        ssi on;
        set $url "http://${WEB_HOST}:${WEB_PORT}";
    }

    # redirect server error pages to the static page /50x.html
    #
    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html;
    }
}
